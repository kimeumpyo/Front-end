import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';  // 외부파일을 불러오는


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);

